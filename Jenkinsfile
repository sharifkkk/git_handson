pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        sh ' echo "this is the build stage"'
      }
    }
    stage('test') {
      steps {
        sh ' echo "this is the testing stage" '
      }
    }
  }
}
